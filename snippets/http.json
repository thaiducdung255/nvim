{
   "API.get": {
      "prefix": ["ge"],
      "body": [
         "GET localhost:$1"
      ]
   },
   "API.post": {
      "prefix": ["po"],
      "body": [
         "POST localhost:$1"
      ]
   },
   "API.put": {
      "prefix": ["pu"],
      "body": [
         "PUT localhost:$1"
      ]
   },
   "API.patch": {
      "prefix": ["pa"],
      "body": [
         "PATCH localhost:$1"
      ]
   },
   "API.delete": {
      "prefix": ["de"],
      "body": [
         "DELETE /localhost:$1"
      ]
   }
}
